If "the" is the most searched term, and "The" is the second most searched term. A user would not be satisfied with those results 
(they would want a different word, so a structure that took caps into account would be better).
Running time would be terrible for a large, unsorted file (at least n, since it goes through the entire list of words).
